export default function NewDeckPage() {
  return (
    <main className="grow overflow-auto px-10 py-8">
      <h1 className="t-section">New deck</h1>
      <p className="mt-6 max-w-xl text-[color:var(--app-text-2)] t-caption">
        Capture the deck brief here — step 1 of the Slideline pipeline
        (prompt → plan → slides → export).
      </p>
    </main>
  );
}
